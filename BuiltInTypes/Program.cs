using System;


    class Program
    {
        static void Main(string[] args)
        {
        bool b = true; // boolean variable can strictly only hold true or false
        byte byter = 155; // bytes can support number range from 0-255 (256 full range). sbyte is -128 to 127. 
        int i = 0;
        Console.WriteLine("Min = {0}", int.MinValue); // bye typing int. , I can see the properties on the integer to see what I have access to with that data type.
        double d = 123.3434; // a type of floating.
        Console.WriteLine(d);
        }
    }
